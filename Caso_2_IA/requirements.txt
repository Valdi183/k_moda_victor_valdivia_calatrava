# K-Moda MMM — dependencias del proyecto
# Instalar con: pip install -r requirements.txt

# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Visualización
matplotlib>=3.7
seaborn>=0.12

# Modelado MMM
scikit-learn>=1.3       # baseline lineal, métricas, preprocesado
statsmodels>=0.14       # OLS, tests estadísticos, descomposición STL
pymc>=5.0               # MMM bayesiano (opcional, requiere pytensor)

# Utilidades
tqdm>=4.65              # barras de progreso en loops de chunks
colorama>=0.4           # output coloreado en scripts de consola
tabulate>=0.9           # tablas en texto plano
